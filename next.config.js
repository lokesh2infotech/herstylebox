/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost', 'k5ovcfkgi9ig5nfk.public.blob.vercel-storage.com'],
    unoptimized: true,
  },
}

module.exports = nextConfig
